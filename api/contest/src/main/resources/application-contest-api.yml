spring:
  application:
    name: contest-api

# Note: spring.profiles.include는 프로파일 특정 리소스에서 사용할 수 없습니다.
# 필요한 프로파일(common-core, mongodb-domain)은 기본 application.yml에서 활성화해야 합니다.

contest:
  internal:
    api-key: ${CONTEST_INTERNAL_API_KEY:default-contest-api-key}
