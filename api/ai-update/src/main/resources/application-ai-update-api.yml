spring:
  application:
    name: ai-update-api

ai-update:
  internal:
    api-key: ${AI_UPDATE_INTERNAL_API_KEY:default-ai-update-api-key}

# Slack 알림 설정 (기본 비활성화)
slack:
  ai-update:
    enabled: false
    channel: "#ai-updates"
