feign-clients:
  github:
    mode: rest
    uri: https://api.github.com
    token: ${GITHUB_TOKEN:}

---
spring:
  config.activate.on-profile: test
feign-clients:
  github:
    mode: mock
    uri: https://api.github.com
    token: ${GITHUB_TOKEN:}

---
spring:
  config.activate.on-profile: local, dev
  cloud:
    openfeign:
      client:
        config:
          GitHubFeign:
            readTimeout: 30000
            connectTimeout: 3000

feign-clients:
  github:
    mode: rest
    uri: ${GITHUB_FEIGN_URI:https://api.github.com}
    token: ${GITHUB_TOKEN:}

---
spring:
  config.activate.on-profile: beta
  cloud:
    openfeign:
      client:
        config:
          GitHubFeign:
            readTimeout: 30000
            connectTimeout: 3000

feign-clients:
  github:
    mode: rest
    uri: ${GITHUB_FEIGN_URI:https://api.github.com}
    token: ${GITHUB_TOKEN:}


---
spring:
  config.activate.on-profile: prod
  cloud:
    openfeign:
      client:
        okhttp:
          enabled: true
        httpclient:
          max-connections: 35000
          max-connections-per-route: 35000
          connection-timeout: 120000 # 120초
          hc5:
            connection-request-timeout: 120000 # 120초
            socket-timeout: 120000 # 120초
        config:
          GitHubFeign:
            readTimeout: 30000
            connectTimeout: 3000

feign-clients:
  github:
    mode: rest
    uri: ${GITHUB_FEIGN_URI:https://api.github.com}
    token: ${GITHUB_TOKEN:}
